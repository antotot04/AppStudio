import { Routes } from '@angular/router';
import { AppLogin } from './app-login/app-login';

export const routes: Routes = [
    {
        path: '',
        title: 'Login Page',
        component: AppLogin
    }
];
